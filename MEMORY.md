工作文件的存储位置为D:\AI_Files；技能的存储位置为C:\Users\29210\.openclaw\workspace\skills。

## 宁的文件偏好
- 给宁发送文件时，必须使用附件形式，不能只发路径
- 使用 feishu-send-file 脚本发送文件（先上传获取 file_key，再发送附件）
- HTML/PDF/Word 等文件都要这样发送

## 问题解决流程
遇到解决不了的问题时，先用 `clawhub search` 查找对应技能；若无对应技能能解决问题，再告诉用户。

## 文件输出规则
新文件放在 `D:\AI_Files\{年份-月份-日期}\`，如当天文件夹不存在则先新建。
例如：2026年3月19日的文件放在 `D:\AI_Files\2026-03-19\` 下。
